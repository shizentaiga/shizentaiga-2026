-- =========================================================================
-- seed_01_master.sql (Service Page Initial Data) v2.7対応版
-- [規約]
-- 1. 時刻指定: unixepoch('YYYY-MM-DD HH:MM:SS') を使用。
--    SQLiteはこれをUTCとして扱うため、JSTから9時間引いた時刻を記述し、
--    コメントに本来のJST時刻を明記する。
-- 2. ID指定: 本番はULIDだが、シードでは可読性のため prefix_名前 を使用。
-- =========================================================================
-- [実行・管理コマンド]
-- SQL実行: npx wrangler d1 execute shizentaiga_db --local --file=./src/db/seed_01_master.sql
-- 一覧確認: .tables / 構造確認: .schema <table_name>
-- =========================================================================

-- =========================================================================
-- [データ整合性の強制]
-- D1(SQLite)はデフォルトで外部キー制約が無効です。
-- 親なき「幽霊データ」の発生を防ぎ、DBレベルで参照整合性を保証するため、
-- 接続ごとに本設定を明示的に実行する必要があります。
-- =========================================================================
PRAGMA foreign_keys = ON;

-- -------------------------------------------------------------------------
-- 1. 店舗（shops）
-- -------------------------------------------------------------------------
INSERT INTO shops (shop_id, shop_name, created_at, updated_at)
VALUES (
    'shp_zenyu', 
    '善幽', 
    unixepoch('2026-04-11 03:00:00'), -- JST 12:00
    unixepoch('2026-04-11 03:00:00')
);

-- -------------------------------------------------------------------------
-- 2. スタッフ（staffs）
-- -------------------------------------------------------------------------
INSERT INTO staffs (staff_id, shop_id, real_name, staff_display_name, created_at, updated_at)
VALUES (
    'stf_shizentaiga', 
    'shp_zenyu', 
    'SECRET',           -- 本名は非公開
    '清善 泰賀', 
    unixepoch('2026-04-11 03:00:00'), -- JST 12:00
    unixepoch('2026-04-11 03:00:00')
);

-- -------------------------------------------------------------------------
-- 3. プラン（plans）
-- -------------------------------------------------------------------------
INSERT INTO plans (
    plan_id, 
    shop_id, 
    plan_name, 
    description, 
    duration_min, 
    price_amount, 
    plan_status, 
    created_at, 
    updated_at
) VALUES 
-- 経営コンサルティング
(
    'pln_consulting', 
    'shp_zenyu', 
    '経営コンサルティング', 
    '課題解決に向けた90分の戦略セッション', 
    90, 
    49500, 
    'active', 
    unixepoch('2026-04-11 03:00:00'), 
    unixepoch('2026-04-11 03:00:00')
),
-- 資金調達プラン 初回相談
(
    'pln_funding', 
    'shp_zenyu', 
    '資金調達プラン 初回相談', 
    '融資・資金繰りに関する専門的なアドバイス', 
    60, 
    11000, 
    'active', 
    unixepoch('2026-04-11 03:00:00'), 
    unixepoch('2026-04-11 03:00:00')
),
-- 顧問契約プラン
(
    'pln_advisor', 
    'shp_zenyu', 
    '顧問契約プラン', 
    '継続的な伴走支援により、組織の構造的課題を解決', 
    0, -- 期間が可変（1ヶ月〜）のためフラグ値として0を設定
    220000, 
    'active', 
    unixepoch('2026-04-11 03:00:00'), 
    unixepoch('2026-04-11 03:00:00')
);